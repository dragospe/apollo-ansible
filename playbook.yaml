---
- hosts: oauth
  # remote_user: centos
  # become: yes
  tasks:
  - name: ensure apache is at the latest version
    yum:
      name: httpd
      state: latest
    become: yes
  # - name: write the apache config file
  #   template:
  #     src: /srv/httpd.j2
  #     dest: /etc/httpd.conf

# TODO: latest version for test, pinned to some "latest working" for prod

# - hosts: databases
#   remote_user: root
#   tasks:
#   - name: ensure postgresql is at the latest version
#     yum:
#       name: postgresql
#       state: latest
#   - name: ensure that postgresql is started
#     service:
#       name: postgresql
#       state: started
