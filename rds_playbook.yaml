---

- hosts: localhost
  # vars:
  tasks:
  - name: get facts about an RDS instance
    rds:
      command: facts
      instance_name: apollo-rds-test
      # apply_immediately:
      # aws_access_key:
      # aws_secret_key:
      # backup_retention:
      # backup_window:
      # character_set_name:
      # db_engine:
      # db_name:
      # ec2_url:
      # engine_version:
      # force_failover:
      # instance_type:
      # iops:
      # license_model:
      # maint_window:
      # multi_zone:
      # new_instance_name:
      # option_group:
      # parameter_group:
      # password:
      # port:
      # profile:
      # publicly_accessible:
      # region: us-east-2
      # security_groups:
      # security_token:
      # size: 100
      # snapshot:
      # source_instance:
      # subnet:
      # tags:
      # upgrade:
      # username:
      # validate_certs:
      # vpc_security_groups:
      # wait:
      # wait_timeout:
      # zone:
    register: new_database_facts
  - debug:
      var: new_database_facts
      # msg: {{ new_database_facts }}
